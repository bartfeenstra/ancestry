#!/usr/bin/env bash

set -Eeuo pipefail

cd "$(dirname "$0")/.."

for locale_directory_path in ./resources/locale/*; do
  language=$(basename "$locale_directory_path")
  pybabel compile -d ./resources/locale -l "$language" -D betty
done
