#!/usr/bin/env bash

set -Eeuo pipefail

cd "$(dirname "$0")/.."

if python3 --version &>/dev/null; then
PYTHON=python3
else
PYTHON=python
fi
$PYTHON -m venv ./venv
. ./venv/bin/activate
export PYTHONPATH=$(pwd)
